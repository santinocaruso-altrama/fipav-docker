# Sviluppo locale: qualunque Host sulla porta 80, in chiaro. Equivalente del
# `server_name _` di nginx - il routing multi-tenant per sottodominio di
# comter (src/proxy.ts -> header x-tenant) continua a funzionare con
# calabria.localhost e simili. Non cambia mai: nessuno script lo tocca.
http:// {
	import site
}
